7.45  指针的比较C7-7.in记事本文件(E)  编辑(E)  格式(0)  查看(  帮助(H)这是一个处理以int main()1.12.2读出buf[]中实数123"行"  为单位的inti,n;数据的各字段并23 4输入数据的基本double d, subsum;把指针指到实数后1.12.23.3框架值得掌握!char buf[BUFSIZ],P;面的第一个空白位6( Windows (CRLF)UTF-8BUFSIZ是一个常for(i=l; fgets(buf, BUFSIZ, stdin) != NULL; it+)char*get_value(char *s, double *d)用的说明缓冲区subsum = 0;大小的符号常量,forln=o, p=buf; (p二Value(p, &d) ) != NULL; ntt)Whilel*s != "10' &&isspacel*s))实际数量取决于subsum +=d;S++; /1  跳过空格,指针指到非空格处编译系统的版本if(n>0)if(sscanffs, "%lf", d) !=1)printf("od:%d %fln", i,n, subsum/n);return NULL;和编译选项,常While(*s != '10' && !isspacel*s))见的数值为512或return 0;stt; // 指针移到实数后的空自位4096。return $;为什么不用scanf输入一行的多个数据?空格和|0都作为空白符处理;不能进行"行"(hang)处理这个程序非常实用,记下来牢固掌握!get