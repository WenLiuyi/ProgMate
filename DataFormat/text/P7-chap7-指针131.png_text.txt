7.3.6 char*型指针与字符串用双引号引起来的字符串常量保存在程序内存中的常量区,它们的内容不能更改字符串常量作为表达式的值是其首字符的地址,具有 const charx类型;可以将它赋值给字符型指针娈量。当通过指针试图更改字符串常量的内容时程序就会崩溃。例如:char*p三Hello" ; // p 指向字符串"Hello'*p三'A'; /1编译没有错误;但发生运行时错误字符串既可以用 char*指针表示,也可以用 char 型数组表示。区别在于前者只是一个指针,指向的内容保存在别的地方中(例如其他某个 char 型数组,某-段常量区的字符串)后者是一个数组,拥有字符串内容的数据实体空间。例如,执行这两条语句:charscanf("%s", s);会导致运行时错误因为指针 $指向的空间并没有被分配野指针)正确的做法应该是char5[180];scanf("%s", s);即,定义一个 char 型数组来确保字符串的存储空间。*5;