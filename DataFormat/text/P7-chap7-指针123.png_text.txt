用数组方式输出数据中最长行的长度和内容例6-10解法)数据中的最长行输入若干行字符串。输出最长行的长度和字符串11二维数组所有元素初始化为'10'程序算法分析 (示例)chararr [2] [MAX_N]二{{""}};目前最长: arr[longest = 0] = "abcd"intin二1,Iongest三0;输入前:arrlin = 11二"ab"intmaXlen二0,Ien, tmp;第2行更短。新的输入存入第2行,即下一条:While(gets(arr[in] )二NULL )输入后:arrlin = 11二"abdefghi"若 len > max_len (新输入的字符串更长)len三strlen(arr[in]);i(len〉len)则 max_len〈len,in 和 longest交换数据。字符数组变为:输入前=arrlin = 0]二"abcd"MaXlen二len;目前最长: arr[longest三1]="abdefghi"tmp二in;保持长的笫2行,下一次新输入存入笫1行,即:in三longest;longest始终待输入:arrlin = 0]二"??"longest三tmp;记录最长行若输入字符串比目前最长的字符串短;不做处理,接着检查下一个输入字符串,存入当前行。printf("%d:%s In'9max_len,arr [longest] );输入文件结束时。输出结果。本设计比较巧妙。不需要拷贝数组 (字符串)|ongest记录己输入的最长行,in表示将要输入的行。max