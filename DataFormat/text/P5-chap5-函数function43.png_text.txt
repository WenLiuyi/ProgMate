局部变量和全局变量的小结与迸一步对比分析(自学)局部变量#include <stdioh>只能在函数内部访问 (函数A的变量*与函数8的变量X即便同名,也是两个完全不同的对象) ,体void useLocal( void );现了函数封装的特征。使得程序容易管理数据安全等。void useGlobal(void);局部(自动)  变量随函数的调用而存在,函数返回后将消失。 从一次调用到下一次调用之间不保int x = 10; // 全局娈量持值,每次调用函数时都重新初始化 (动态分配)int main0变量定义时未初始化,则没有确定的数值读取前应赋值 (经常容易忘记! )inf x = 5;//局部娈量约定俗成:  循环控制变量常用 ; jk等循环次数娈量常用m n 等 (不是C语言的要求,而printf(" OdW", x);是编程人员的习惯或团队的要求等)useLocal0;useGlobal0;形式参数是局部变量。0refurn 0;全局变量void useLocal( void程序运行时始终存在,定义 (或声明)  后的位置都能访问。inf &=25;若末初始化。则默认为0。prinff(" OdW"++8);常用于定义常量变量 (如数组大从N等系统配置数据等)或多个函数交换数据时的共同娈量。void useGlobal( void在函数中尽量避免或减少对全局变量的修改 (便于程序调试和维护)8 *=10;printf(" Odl", 8);个文件中,全局变量与函数的局部变量可以同名,但不建议这么做 (变量命名习惯? )