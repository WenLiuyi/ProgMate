7.1 指针,地址,变量内存之间的关条任意的地址都可以个合法的具有指针类型计算机的内存空间以字节为单位编址。对作为指针的值吗?NO的数据必须指向一个完整于单位长度为多字节的数据实体其地址的数据实体。是其第一个字节的地址 (低地址端二doublea,b;printf("%XIn&a);6IFEF8printf("%XIn'&b);GIFEFOint1;printf("%XIn'&i );GIFEECshortX y;printfc'%XIn'&X);61FEEAunsignedintarn[6];printf("%XIn"&y ) ;输出6IFEE8printf("%XIn'arr ) ;6IFEDOchar s[8];printf("%XIn'S);GIFEC8地址偏移量地址base OXO0XI0X20X30X4OXS0XG0X70X8OxgOXA0XBOXCOXDOXEOXF哪些地址可OXGIFEC$[0]8[1]$[2]$[3]$[4]$[5]8[6]8[7]作为合法的OXGIFEDarr[0]arr[l]arr[2]a11[3]指针值?OXGIFEEaIT[4]arr[5]Y又OXGIFEFba娈量在内存中的分配情况