#include〈stdio.h>两种方式的对比#defineMAXLINE1024/*数组版*/intStrchar S[ ] );#includecstdio.h〉VoidStrCOPychar s[],char t[ ]);#includecstring.h>/*指针版*/#defineMAX188int main()11findlongestlineint main()int len,max;当前行;目前最长行的长度chararr_I[MAX] ,arr_2 [MAX];charline [MAXLINE ] ;11  保存当前输入行  的内容CharIine二arr_1,*longestCarr_2,charsave [MAXLINE ] ;/1  保存最长行的内容intmax_len0len;MaXWhile(gets(in_line )! =NULL )while( gets(line)!二NULL)lenzstrlen(in_line);len二str_len(line);if(lenmax_len)i(lenmaX)MaXlenClen;maXlen;tmpin_line;StrCOpySaVe,line);in_line二longest;longest二tmp;i(maX}printf("%d:%smax,save);printf("%d: %sIn"max_len, Iongest);return0;与数组实现方式相比。指针实现方式减少了每当发现新的更长行时所进行的字符数组拷贝(通过调厢函数str_copy)。 显然指针实现方式代码执行速度要快。Llen*in_*tMP;