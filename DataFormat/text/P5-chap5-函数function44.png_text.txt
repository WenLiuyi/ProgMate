局部变量的补充说朋C89标准规定一个函数中所有局部变量必须集中定义在函数体中第一个执行语句的前面而不能穿插在执行语句之间#include 〈stdio.h>遵从C89标准的编译支持C99的编译系统接受这样的系统会报错int mainO书写现在的绝大多数编译器都支持 (一般不会遇到问题)?it X;gcanf("Ood",较老版本的C8, Devcprintf("%odln" , X);会提示仅支持C99模式。按C99风格,写程序会更灵活些。double y;按C89风格,易于对娈量集中管scanf("Olf" , &y);理;  也会更保险些 (现在的编译小iti;printf("Oofln" , y);器基本都对旧的标准兼容;  按for(int i=l; i5;1++)Printf("Od,j;return 0;C89风格跟老的程序兼容性好。&x;