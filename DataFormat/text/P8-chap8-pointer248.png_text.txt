8.7.2函数指针用祛与惠义例 8-13通用冒泡排序函数 写一个通用的冒泡排序函数,可以对任何数据类型的数组进行任何形式 (升序或降序)  的稳定排序,具体的数组元素比较行为通过函数指针实现。(2)冒泡排序的通用函数实现Void g_bub_sort(void *array,int Ienint elemsize,int(*cmp)const void* ,const void *))inti j;for(1二1;1<len;i++)1 1bubble sortingfor(i二 0; j<leni;j++)i(cmp(array十(i+ 1*elemsize,array十j * elemsize) )swap_elem(array十j * elemsize,array十(j+ 1米elemsize,elemsize) ;if 语句是通过函数指针调刖比较函数来判断元素 j和元素 j+1是否有序,如果不是有序则交换它们的内容(如果j+1 元素应该排在 j元素的前面,cmp 会返回真   表示 jj+1元素不是有序的)函数指针指向的实体将在函数调用时确定。