条伴语句实例: 谆点数的比较#include〈stdio.h>找bu9计算机中有些实数的表示不是100%#include〈math.h>intmain()精确;存在表示误差。特别经过计doubleb,C,p1p2,dt;算后。误差会放大Scanf("%1f%lf%lf"0&a, &b,&c);对于实数而言,理论上等于某个值时dt二b *b -4 *a * C;讦(dt〉8.0)可能实际存储存在一定的误差rl二(-b十sqrt(dt))/(2 *a);很多情况不要对实数进行 == 或 !厂2三(-bsqrt(dt)) /(2 *a);的判断printf("tworealroot: %f %fIn'9p1,p2);可以根据问题中数据的精度情况。判断elsef (0.0三=dt)是否在精度范围之内printf("onerealroot: %fIn"-b / (2 *3));else如:  数据精度保留到小数点后9位有效printf("norealrootIn" );数字则 (dt != 0)可改为 fabs(dt)>二1e-9 (此时需要包括 <math.h > _fabs为求实数的绝对值)a,