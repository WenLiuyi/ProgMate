标准输入 /输出(IO)重新定向it i, n, sum=0;标准10在默认情况下均对应控制台(从标准for (n=0; scanf(" Ood" , &data[n]) != EOE; n++);for (i=; in; itt)11 show your input设备层面看,则分别对应键盘和显示器,也Printf("%odln"data[i]);printf("Inln");可以把标准|0重新定向为文件)/1 get the sum and average当程序需要对标准输入/输出进行大量读写for (i=0; in; 计+)sum += data[i];时。如:  需反复从键盘输入大量数据  (输入10000个以内的整数。求和。求平均)printf("num: %dl" _n;printf("SUm: %dln", sum);printf("average: %.2fln"(float)sum/n );大量数据反复测试96多次重新输入8573重复劳动,且极易出错91