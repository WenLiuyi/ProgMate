7.3.6 char*型指针与字符串字符串是以10'结束的字符数组。例如 char s[100]"hello"定义了一个字符串。数组s的内容为字符串"hello" (实际的存储内容最后还有一个"10')数组名 $作为表达式的值 (例如出现在赋值符的右侧)  是该字符串首字符的地址,可以通过隐式类型转换为 char*类型。0若有一个 char*型指针 p 指向-一个合法字符串的首字符,则通过 p 可以遍历访问这个字符串的每个字符 ("0'为结束标志),即 p 完全能代表这个字符串。在没有上下文歧义的情况下。可以简称指向合法字符串的 charx型指针为字符串例如定义 char *p = s, 则 p 也是字符串。而且和S是同一个字符串(指向同一个字符数组)Char*p二NULL,S [20]三pointtOme〉p二"pointtOme"; // p 指向字符串常量" pointtOmep = $;11 p 指向字符串5的内容5三"pointto you"; // 编译错误;5是数组名,是右值,不能被赋值D三pointtOyou" ; // p 指向另一个字符串常量"pointtoyoU