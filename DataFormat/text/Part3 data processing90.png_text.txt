例#include〈stdio.h〉#define [ArrayNumX(sizeof(X)/sizeof(X[8]) )在每个循环中,控制循环次数的语句#defineFOR(i,S,N)for(i=s;iN;计++)int main()都样 (替换为相应需要处理的数组int i;名) ,而不用关心每个循环中的实际double d[7];char c[26];float f[10];次数 (不需要每个循环处用相应的常for(i=0;1ArrayNum(d)i++ )量)程序维护方便可读性强。d[i]二sqrt(i+10) ;printf("%fInd[订]);for(i=0;1ArrayNum(c i++ )妙用define但初学者慎用用得太c[i]二1 + 'a';多,程序的可读性可能也不好!printf("%cc[i]);}printf("In" );for(i=0;iArrayNum(f) ;i++)1/可用宏FOR 代替f[i]二1 *1;此处的 for 循环头可以用宏printf("%fIn"〉f[]]);FOR(i,O,ArrayNum(f))来代替但可读性不一定适合每一个人。