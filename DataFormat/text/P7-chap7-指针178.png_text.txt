7.45  指针的比较从标准输入上读入以空格分隔的字符串 s和t,将$ 中首次与t匹配的子串逆置后再输出 s,当S中无与t匹配的子串时直接输出 $。abclz3defg 123abc32ldefg输入样例:abclz3defg 234输出样例:abclz3defg#include〈stdio.h〉Voidrev(charxfirstCharxIast )#include 〈string.h〉Voidrev(char米char *);inttmp;intmain()While(firstlast )charstr[BUFSIZ],substr[BUFSIZ],*p;scanf("%s%s"str substr);tmpC*last;if((p三strstr(str,substr))! =NULL )*last三*first;rev(p,P十strlen(substr)-1 );*firstCtmp;puts(stn);firsttt,last--;return0;为何不直接调用strRev?而要重写rev函数?