例5-9:设计万年历(函数实现)主函数简简单单。清清爽爽。每个函数实现特定珈能;实现更清晰,更易于维护!甚至利用了以前写的函数。减少重复劳动。如getWeekDay(), isLeap()等。int getWeekDayint year_〉int month,int day);11查询某天是星期几int isleap(int year);/判断是否是闰年int printyearcalendereintyear,int weekday); 打印年历int getDaysofMonthintyear,int month);11  获得月天数int printMonthcalendar(int month,int days,int weekday );代码复用,省力;intmain()1 main函数清清爽爽单一责任;接口简洁;int year, weekday;意义明确。格式优雅;printf("Ininput year:");scanf("%d&yean) ;调试方便。扩展便捷。weekday=getWeekDay(year,1。 1);11求元旦是星期几printyearcalender(year,weekday ) ;11  打印年历return0;