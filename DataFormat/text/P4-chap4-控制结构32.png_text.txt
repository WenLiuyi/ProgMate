多路选择结构 :switch开关语句switch可以地进行多项选择语法为:switch括号中的控制表达式与每个case标记比较若与某个标记匹配就执行相应的语句switch (表达式)若连续的case之间没有语句则它们执行同样的语句与所有ca5e都不匹配时,执行default对应的语句(有时Case常量表达式1:语句块1;省掉。作为好习惯建议保留)Case常量表达式2:语句块2;switch的表达式结果必须是整型值Case常量表达式m: 语句块n;case标记值只能是整型常量或返回常量的表达式 (不能是娈量)Case常量表达式h: 语句块";break语句:使程序跳出switch, case中有多个语句时default: 语句块n+1;不必放在{ }中,因为会按顺序依次执行