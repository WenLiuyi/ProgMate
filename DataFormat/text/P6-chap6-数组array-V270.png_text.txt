字符串和字符数组的关条字符串与字符数组很相似但又相异两者关系很密切很多应用可以互相替换但两者又有区别(串是'常量数组是 "娈量" )注急木能苴换与混淆。字符数组可用于定义字符串但未必所有字符数组都是字符串只有以 'I0' 结尾的字符数组才"被认为"  是字符串。否则只能称之为字符序列。数组可以是int, float char等很多类型。字符串可以看成一个char类型的数组。字符数组:s1,82,83都是字符数组char S1[ ] = {""first" }; // 用串常量"first" 初始化字符数组 s1char S2[ ] = {'f。'i,''。's','t。"10'};$1和s2可作为字符串处理char S3[ ] = {'f。'让'。'1'。's'。'f};83不是字符串字符串字符数组常量或娈量常量娈量定义方式"first"char s[]二first"/* 用字符串常量对字符数组初始化,first'是常量,但s[]是娈量。printf("Os", $);三printf("%s"'first");* |读写方式只读当无const限定时可读可写结束标志"I0'无要求