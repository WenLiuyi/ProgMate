6.1.4数组的复制数组的复制C 语言不支持数组直接整体复制即不能把数组整体赋值给另一个数组。1 int a[12],b[12];数组操作的注意事项2b=3;//数组整体赋值。错误!数组元素可以当成普通娈量进行相应操作,3让(b ==a )/1判断两个数组内容相等。错误!比如赋值。比较等,但数组名不可以这样直11方法1:  通过循环逐复制数组中元素接操作。1 for(iz0;i < 12;计+)2b[i] =a[订]C 语言不允许直接对数组进行整体操作。3 for(i = 0;1< 12;1++)memcpy标准库函数{(a[]]二二b[订])void *memcpy(void *dest, Void *src, size_tcount);;撂缴4嵩:个字节拷贝到dcs效率高!实现内存第一个参数是目标数组 ,第二个参数为源数I1方法2:  通过内置函数memcpy0实现整体复制组第三个数组是需要复制的字节数memcpy(b, a, sizeof(a));使用函数 memcpy 需要包括头文件〈string:』>