8.1指针与数组指针与数组的关系非常密切,具体体现在以下两点1数组名出现在表达式中,可以隐式类型转换为指向数组首元素的指针。例如 int 型数组名可以看作指向数组首元素的 int*型指针,因此数组名可以参与指针运算,可以赋值给同类型的指针变量。注意数组名是右值。不能对它赋值或改娈它的值,如:float a[10];float *p = a; //p 指向数组a的第一个元素ptt; // p 指向数组a的第二个元素a = p +1; //编译错误数组名不能被赋值3++;11编译错误:  数组名是右值,不能用于++运算