7.45  指针的比较两指针间的比较:  两个指向同一类型的指int array[N],*P,针,可以进行!=。=二,>, <"等关系运D=&array [0];算其实就是地址的比较。q&array [3];WeCancomparepWithq在指针未指向任何实际的存储单元时或指针所指向的存储单元已经不存在时通常将其标记为空指针NULL。空指针NUL在c的标准头文件中是一个值等于 0的宏定义野指针:  指针指向没有分配的空间造成访问隐患, 称为野指针,例如定义时未初始化。指针经常和NULL使用==; !=进行比较判断指针是否为空(无效)*q;