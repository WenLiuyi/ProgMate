6.2.3插入排席(自学)例 6-7经典的插入排序算法1 void insertSort(int x[], intn)例6-7代码分析2第4行外层循环,控制排序趟数, i从1到n-13inti,j, temp;for (1=1;1<皿;计+)共n-1趟。第6行,用临时变量 temp 记住待插入的第i+1;temmp = X[订];个元素的值。for (j=1-1;j>=0;j-)第7行。从第i个元素开始向前遍历有序子数组 _8寻找 temp 的插入位置9让 (x[j]temp)10x[j + 11 = X[j];第%行,判断遍历位置j上的元素与 tenmp 的关11Clse系,如果比 temp 大,则 temmp 应该在它前面;因12break;此将j元素后移(第10行) ;否则找到temp13的插入位置为j+1,并跳出内层循环。14X[j + 1] = temp;第14行,将temp 值 (即内循环开始前的第i+11516 }个元素值)写入到数组下标j+1的位置