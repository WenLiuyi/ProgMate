7.4.1指针加臧鏊数指针既然可以与整数进行加减运算。那么作用于指针娈量的++十二-=运算符也是合法的:p+ +等价于p =p+1;p--等价于 p = p-1;p+=n 等价于p = p + n;p -= n 等价于 p= p -n。下面的例子展示了指针加减整数的运算:inta[5]三{10,11,12,13,14};int*pi三&a[1];11 pi 指向3[1]pitt;1 pi 指向a[2],xpi 为a[2],即12pi十二2;1 pi 指向a[4] ,*pi为a[4]。即  14(pi-2)=*(pi-4);11 *(pi-4 )为a[0],米(pi-2 )为a[2],相当于a[2]二a[0];Constchar*p二BUAA"十/1 p 指向常量字符串 "UAA'只要是合法的指针,用*号对它进行解引用就是访问它指向的变量上述代码中 *(pi-2)= *(pi-4)的赋值语句,因为解引用运算符的优先级高于加减法因此一定要括号来保证先进行指针加减再解引用。1;