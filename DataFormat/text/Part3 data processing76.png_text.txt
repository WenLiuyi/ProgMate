例3-9/求ax2+bxtc-0 方程的解,按如下四种#include〈stdio.h〉情况处理:#include〈math.h〉#defineeps1e-101.a=0,方程不是二次方程int main(){double a,b,C,delta, XI, X2, realpart, imapart;2.b2-4ac=0,有两个相等的实根scanf("%If%lf%lf" ,&b, &c);if(a二二3.b2-4ac>0,有两个不相等的实根printf(Not3quadratic" ) ;else4.b2-4ac<0,有两个共轭复根delta三b*b-4*a*c;if (delta二二printf(Two equalroots:%8.4fIn" , -b/(2*a) );浮点型数据在存储和计算时会存在一些微小的误差else if(delta〉0)因此,对浮点数的大小比较一般不用"=="或"!='XI(-btsqrt(delta) ) /(2*a); //a很小时,可能溢出X2二(-b-sqrt(delta) ) / (2*a);而是应该用大于或小于符号。11  打印实根(略);}else代码中a0和 delta这两个地方可能带来问题11  计算。打印虚根(略);采取的办法:  判别实数之差的绝对值是否小于一个很小的数 (比如1e-6)则return0;(delfa虢0)可改为 fabs(delfa)<CDS&a,