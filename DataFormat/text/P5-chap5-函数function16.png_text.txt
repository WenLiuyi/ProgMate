函数定义方式方式一: 先声明后调用再定义方式二:  先定义后调用 (函数定义中的函数头也同时作为函数原型)#include〈stdio.h〉#include〈stdio.h〉long long LL_pow(int,推荐风格long Iong LL_pow(intXsint y)int main()函数声明man函数是函数定义且声明long ans二1;intX,Y,N;int1;scanf("%d%d" _&N);C 程序唯for (1二1;  1<= y;i++)for(y二1; Y <=N; ytt)ans*=x;printf("pow(%d,%d)二%lIdln" _9X,y,LL_POw(X, y));入O, 其它returnans;return0;函数调用函数直接或int main()long longLL_POWintX,int y)间接被mainintXsY,N;1111scanf("%d%d'&N);long Iongans二1;函数定义 (实现)函数调4后for(y二1; Y <=N; ytt)int1;printf("pow(%d,%d)二%IIdIn")XY,LL_pow(X,y) );for(1二1;  1<= y;1++)ans*X;才能被执行return0;return ans;'先声明(接口清晰)后定义 (可能交给他VS先定义细节后按需调用头重人完成)快速定位到程序的主体ma"函数脚轻削弱了main函数主体地位int);long&X〉&X〉