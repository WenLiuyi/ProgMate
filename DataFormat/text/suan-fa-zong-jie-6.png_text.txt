priority_queue优先队列(堆)注意重载运算符。堆默认为大根堆,但是用小于号实现的,如下定义小根堆1priority_queuecintvectorcint> , greatercint>>9;pair可以用来代替一些便捷的自定义structEpair自带小于号,可直接用于排序。第关键字为第一维升序。第二关键字为第二维升序1rcint, int>p1;2paircint, string> p2;3paircdouble, int>p3;)HnLL/Vector1Vectorcint>V;2V. push_back() ;3V.size0);4for(intz0;i=V.size0)-1;计++){//错误光明正大5cout<<V [订一16}7for(int i0;i(int)v.size0)-1;+++){1/(正确)118coutc<V [订] <<'9}Set集合无重复 有序1Setcint>5;2//插入3for(int il;i=n;计++)4S.insert(gint0));光明正大5//遍历6for(autoX5)11117cout<<X<<'8s .begin() : 返回笫一个元素的迭代器9S.end () : 返回最后一个元素后的迭代器10//故也可以这样遍历11for(autoit=s. begin() ;it !=s.end(0) ; it++)12cout<<(xit) <<135。1ower_bound (x) : 返回第一个大于等于X的值的迭代器map构建一个映射关系复杂度为 O(lo8n)pai<"