函数返回值int max(inf x, inty)int ma8 = &;返回计算结果a(Yn88)max =y;retum语句三种功能return max;返回函数的计算结果如@返回函数的执行状态,般是具有逻辑判断功int is_even(int x)int is_even(int x)让 (892 ==0)能的值。如0_3return 1;return &%2 == 0;elsereturn 0;无返回值强制退出当前函数如(int isLeap Year(inty)8带有返回值的标准函数示例relurn@4==0 && yOlOO!=0) IlyO400z=0 );根据返回值设计条件表达式;scanf 返回int 类型,则while(scanf(.. ) != EOE返回逻辑状态getsChar*类型;则while(gets(..) != NULL)void prnLeap(inty)strcnPint 类型,则if(strcmp(strl,str2) ==0)离开函数{( isLeapYear(y) )isdigit 返it 类型,它具有逻辑判断能力,则printf(" Odis a leap,y);i(isdigit(c)>refurnprintf(" Odis not ayear'y;year'leap