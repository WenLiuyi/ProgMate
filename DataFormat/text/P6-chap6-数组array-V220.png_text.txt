6.1.7 娈长数组及其隐患娈长数组及其隐患1变长数组的隐患1 #include本代码在支持 Cgg 标 引起爆栈现象:  娈长数组的空间分配在函数调用栈2 int mainO准的编译器上没有问上 (变长数组只能是局部数组,不能定义成全局数题。但在 Cgg 标准之3前的编译器上会报编组) ,而函数调用栈的长度有限(windows 平台下4 itn,i sum=0;译错误5 scanf("god" , &);一般小于2M)但在运行时n的大小是无法预料6 int a[];的一旦n过大。将导致在函数调用栈上开辟了过7 for (i= 0;i<n;计+)8 {大的空间,程序会崩溃退出。9scanf("Od" , &a[订);使用条件:  在支持 Cgg 标准的编译器里使用娈长10sum += a[订;11数组时。要预估数组长度,确保不会发生"爆栈'12printf("%.2f"SUn*1.0/);的情况。13return 0;为了避免运行时的错误尽量避免使用娈长数组。14