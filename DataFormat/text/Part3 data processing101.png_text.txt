对标准输入输出文件重新定向freopen('c3-Il.daf'rstdin);for (n=0; scanf("%d" , &data[n]) == 1; n++);示例中不再从键盘读入数据,而是从文件for (i=0; in;itt)C3-11.dat中读入数据c3-11.dat就相当printf("Oodln'data[i]);于新的stdin (标准输入)Printf("Inln");10文件重定向的含义:  在不对程序输入/输出语句做任何改动的情况下使程序从指for (i=0; in; itt)sum += data[i];定的文件中读入测试数据(整体读入,分批处理)并将结果写入指定的文件。如printf("num: %dln",n);将对键盘和屏幕的读写改为对指定文件printf("sum: %dln"SUm);printf("average: %.2fln"(float)sum/n );的读写操作。好处:  对C程序内部处理逻辑无任何影响,fclose(stdin);可避免重复键入测试数据。