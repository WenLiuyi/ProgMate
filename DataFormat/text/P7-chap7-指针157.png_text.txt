7.4.1指针加臧鏊数因为自增自减运算符++--与解引用运算符*的优先级相同连用时按照从右向左的顺序结合。例如*0++等价于*(p++),由于自增运算符出现在指针右侧。意味着先对指针 p 进行解引用。再对p指针进行自增。如果要对 p 指向的娈量值进行自增则需要写成(*p)++。char *strcpy(char米dest,char*SrC )Char*d弓dest;While (*d++三*SrCt+)! ='8' );return dest;从右向左结合*p+ + 等价于 *(0++),由于是后置++运算。先执行*p,然后再自增1地址增加)*p)+ + 表示p先与*结合++作用于变量所指向的变量是娈量值增加)