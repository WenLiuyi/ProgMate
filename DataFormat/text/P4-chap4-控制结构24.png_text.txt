嵌套选择结构的程序效-分析三88;性能提示让 (grade >= 90)嵌套 if...else 结构比一系列单项选择 if 结构运行速度快迂CIradc"A180 && grade90)得多,因为它能在满足其中一个条件之后即退出。printf("BIn");让grade >= 70 && grade80)在嵌套 if...else 结构中测试条件中 true 可能性较大printf("CI");的应放在嵌套 if...else 结构开头,  从而使嵌套 if...else让grade >= 60 && grade70)printf("DIn");结构运行更快比测试不常发生的情况能更早退出。让 (grade60)printf("Fln");例]若成绩好的学生编到实验班,要打印实验班的成绩等级 ,哪个程序更快?三88;二88;让 (grade >= 90)让 (grade < 60)Printf("AI");printf("Fln");else 迕grade >= 80 )else 迕grade < 70 )当然现在的普通个人电脑的计算能力足够强大;能printf("BI");printf("DIn");else 让grade >= 70 )else 让<80 )计算 ">230"  次/秒,右图程序计算速度的区别我们printf("CI");printf("CI");根本感觉不到。但养成好的思维习惯总是好的。比如else 迕>二60 )else 让<90 )printf("DW");printf("BW");同样的逻辑循环执行  230次呢?elseelseprintf("FW");printf("AIn");gradegradegradegradegradegradegrade