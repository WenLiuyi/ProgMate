7.4.1 指针加臧鏊数_自定义一个字符串查找函数。实现标准 C 函数 strstr 的功能。问题分析:设计函数的接口和功能:Char火StrStr(const char *buf const char *str)从字符串 buf 中寻找字符串 str, 如果找到以指针形式返回 str 在 bbuf 中第一次出现的位置。否则返回空指针。为简化,不考虑输入的边界条件保证 buf 和 str 不为空字符串。#include〈stdio.h>#defineN1888char米Strstr(constchar米constchar*);intmain()char buf[N十5],str[N十5];char*pbuf三buf,*pstr三str;(pbuf);11保证输入不超过定义的字符数组宽度gets(pstr);11  保证输入不超过定义的字符数组宽度char*p三Strstr(pbuf,pstr);D?printf("%dIn" _9Ppbuf)printf('no foundln");return0;gets