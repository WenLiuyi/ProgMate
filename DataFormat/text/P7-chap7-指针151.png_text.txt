7.3.9 指针作为函数返回值#include〈stdio.h〉有了辅助函数 print_str。可以设#include 〈string.h>计出主程序如右所示:char buf[1805];charstr[18e5 ];void print_str(constchar *pI,constchar *p2);int main()读入一行字符串,包括行尾的换行符char*p1三buf,*02二0;fgets(buf,1880,stdin);scanf("%[^In]" ,stn);读入一行不包括行尾换行符的字符串While(p2二strstr(pi,str) )作为待查找的子串print_str(pI,p2);printf("IloveC");p1跳过要替换的字符串,指向后面p1二p2十strlen(str);的位置fputs(prstdout );return0;