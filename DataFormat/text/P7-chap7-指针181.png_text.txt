7.4.6  指针的强制类型转换将-个 int 型数据由小端字节序转换为大端字节序。问题分析:C 语言在 Intel 架构的 CPU 下编译出来的指令为小端存储字节序,即娈量的低字节放到低地址端,如例7-13中intb二0x12345678,则娈量 b 的值在内存的存放情况 (假设 b 的地址为0x61FE4)见下图所示。int b = 0812345678;TCPIIP 网络通信协议采用大端存储字节序,b占的空间即接收到的多字节流中低地址端存放的是高b的地址字节的数据 (跟人们从左向右阅读的习惯一GIFEI4OrGIFEISOz6IHE16OxGIFE17致)因此通过 C 语言编程向网络传输数据78563412时 (例如通过 TCPIIP 协议)  需要进行字节序转换。b的在值在内存中的存储情况:  娈量的低字节放在低地址端