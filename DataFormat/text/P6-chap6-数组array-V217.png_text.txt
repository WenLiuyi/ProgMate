6.1.6 一维数组作为函数参数例 6-3用函数计算向量点。写个函数,计算两个 double 类型三维向量的点积。几a = (a1, a2, ... , On)例6-3代码分析0 =丫aibib = (b1'笫7行,调用 dot 函数时,将数组名a和b分别1 #include <stdio.h>赋值给了形式参数u和 V, 在 dot 函数内执行时2 double dot(double u[3], double u[3]);对u和 v 的访问就相当于对数组 a和b的访问;3 int maing第11行~14行,dot 函数定义中,形式参数u和 V45double a[] = {1.0,2.0,3.0};用于接收传递的数组名;6double b[] = {1,1,1};7Printf("%.2fln"dot(a, b));调用函数时实参直接使用数组名"不能"  包括数组8return 0;9长度,如,不能写成 dot_Vec(a[3], b[3]. ..)。如果把参10数写成a[3]这样传递的就是数组元素  (普通娈量)11 double dot(double u[3], double V[3])数组传递时数组名作为实参,仍然是值传递,它将数12组的首地址传递给形参即把a的值 (8a[0]) 传给 U13return U[O]*v[0] + u[l]*v[1] + 0[2] * V[2];14 }对 4的访问,是从a 的地址开始访问。输出结果6.00,b12,.. =