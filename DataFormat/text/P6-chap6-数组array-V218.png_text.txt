6.1.6 一维数组作为函数参数double dot(double u[3], double V[3])数组参数的长度return U[O]*v[0] + U[l]*v[1] + u[2]头V[2];维数组做函数的形式参数时数组长度等价可以省略。严格来说是编译器会忽略数double dot(double u[], double V[])组作为形式参数时的数组长度,但数组的return u[O]*v[0] + U[1]*v[1] + u[2]火V[2];方括号标记不能省略;#define ARRAY_LEN(a) (sizeof(a)lsizeof(a[0]))数组作为参数,通常也需要额外定义一个double dot(double u[], double V[])形参,来传递数组长度it in = ARRAY__LEN(V);作为函数形式参数的数组名,虽然形式上double ret = 0.0;for (i=0;i<n;计+)不管数组V的长度是是数组,但本质是 个指针类型娈量(将ret += U[订] * V[i];return ret;多少,为什么n 总是个固定的常数?在第七章介绍)