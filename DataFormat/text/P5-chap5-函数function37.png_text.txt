变量肘间域#include〈stdio.h>时间域是指变量在内存中的存储时间intX;int test();C语言通过存储类型确定娈量的时间域int main()存储类型决定娈量的存储位置和存在状态,常用的有4种printf("A:%dIn" _X);aulo:默认省略。娈量存储在动态内存,其时间域是从intX二5;printf("B: %dln" _X);定义变量的函数或语句块开始执行时刻到执行结束时刻X三test();register: 变量存储在寄存器 [*]int义三7;static: 娈量被存储在静态存储区域*]printf("C: %dln",X;extern:引用被存储在另一个文件中的全局娈量 [*]printf("D: %dln",X);带存储类型的娈量定义格式return0;inttest()存储类型  数据类型变量名;intX = 3;returnX;