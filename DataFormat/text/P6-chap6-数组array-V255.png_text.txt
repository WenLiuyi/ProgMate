6.3.2折半查找例 6-10代码1 int recBinFind(int b[], int key, int low, int high)例6-10代码分析2 {第4行,递归的基本条件,如果下标下界3it mid;b是待查找的数组。low 比上界 high 还要大,说明没有找到,返4让 (lowhigh)key 是关键字low 和 high 在数组中回-1;  否则,程序继续往下执行,开始折半5retumn -1;查找范围的上下界查找。6mid = (low + high)/2;长度为卫的数组。调7让 (keyb[mid])用时传给函数的上下第6行,计算中间元素下标 mid。8return mid;界为0和11第7~8行,如果中间元素值等于关键字查9else 让 (keyb[mid])找结束。返回关键字下标。10retumnrecBinFind(b, key, mid + 1, high);第%~10行。如果关键字大于中间元素,递11else归查找数组后半段。12IetUmhrecBinFind(b, key, low, mid1);第11~12行,如果关键字小于中间元素,递13 }归查找数组前半段。