#include〈stdio.h〉妙用两个函数这里用法很巧妙#include 〈ctype.h〉getchar0数组元素的下标与字母的关系#defineN26islower0'a' - 'a'一>0/hash娈换。把字符映射到数int main()'6' -'a'一>1,inti,C;int upper=o, total-o,lower[N]= {8};while((c=getchar())!=EOF)数组元素'整型)  用于计数{lower[o]计'a'出现次数;if(islower(c))lower[C -a]++;讦C is'alower[0]++lower[l]计 'b'出现次数;else if(isupper(c))uppertt;lowerlc- 'a']++; 等价于totaltt;|i(ca' ==0)for(1=0;iN;i++ )hash变换。把数映射到字符lower[0]tt;J(C - 'a' ==1if(lower[i]!=0)[printf("%c:%dln计alower[i]);lower[l]tt;printf("Upper:%dInTotal: %dln'upper,total ) ;return0;课后练习: 给出标准输入字符序列,。统计有多少个单词?