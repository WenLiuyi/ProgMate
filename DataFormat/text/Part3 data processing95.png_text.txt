用变量定义数组大小=课后读物*]C语言(C89标准)  不支持动态数组,即数组的长度必须在编译时确定下来而不是在运行中根据需要临时决定。但C语言提供了动态分配存贮函数。利用它可实现一种常见的用法:动态申请空间。先定义宏常量以宏常量(1)在 ISOIIEC98gg 标准的 6.7.5.2 Array declarators 中明确作为数组长度说明了数组的长度可以为娈量的称为变长数组(VLA,Variable length array)(注:  这里的娈长指的是数组的长度#defineLEN10是在运行时才能决定,但一旦决定,在数组的生命周期内就不会再娈。 )intmain()(2)在 GCC 标准规范的 6.19 Arrays Of Variable Length 中指出,作为编译器扩展,GCC 在 Cgo 模式和 Ctt 编译器下遵doubleS [LEN];守 ISO Cgg 关于娈长数组的规范。(3)C89是美国标准之后被国际化组织认定为标准C90,除了标准文档在印刷编排上的某些细节不同外,ISO C(CgO)和 ANSIC(Cgg) 在技术上完全一样。