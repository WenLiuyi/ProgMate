7.3.6 char*型指针与字符串对于字符串常量本质是个 N0' 结尾的字符数组,存放在程序的静态存储区,字符串常量作为表达式的值是该字符数组的首地址,可隐式转换为const char * 类型。同样内容的字符串常量不管在代码中出现多少次,都是同-一个对象(内存)printf(ea constant character stringI ' ); 传递给函数的是字符串常量的首地址。注意: char数组和char*型指针使用时容易混淆!(见下例)char *char_ptr; word[20];char a[]="hello" *p="hello";/1p 指向字符串常量char_ptr"point to me"//正确.把字符串常量的首地址赋给指针娈量。a[0] = 'c; /1word只"you can't do this";*p = 'C';/错误Word是数组名,不能赋值正确做法为:/1错误;不能修改字符串常量内容strcpy(word,");