5.2定义函数例5-2  自定义函数pw计算&的y次方为整数)#include〈stdio.h>函数类型long long LL_pow(int,int);函数声明标准函数 (库函数)由C语言定义可直接调用int main()但要用include命令引入包含其函数声明的头文件intX, Y, N;函数调用自定义函数:  用户根据特定需求自己实现,不用包scanf( "%d%d"&N);含头文件但需要用户自行定义函数for(y二1; Y <=N;yt+ )printf("pow(%d,%d)二%IIdIn"〉Xy, LL_pow(X,y) ) ;#include〈stdio.h〉return0;#include<math.h>intmain()函数声明位置函数定义(实现)long LL_powintXsint y)intXsy, N;scanf("%d%d"&N);函数调用long longans二1;int1;forY二1; Y <=N; ytt )for(1二1<= y;i++)白箱:  自定义printf ("pow(%d, %d)二%.Ofln'〉XY, pow(Xy));ansX;return;return ans;函数实现黑箱:  库函数(孓sJ&X〉long&X〉1;*二