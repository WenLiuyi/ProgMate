6.5.4标准字符串处理函数例6-20函数 strncpy 操作示例1 char str[20];2 char Str2[10];3strcpy(str; "hello world");4strncpy(str; "hello world", 20); /1 等价于 strcpy(str;hello world");5Printf("OsIn"Str);6strncpy(str2, str, 10); /1 注意,str2 不是一个合法字符串7/Printf("@sI'str2); // printf("%s",.. )只能作用于字符串,但 str2 不是字符串例6-20代码分析笫3行,将字符串常量"hello world"的内容复制到 str 数组中,Str也是一个字符串。笫4行由于"hello world"的字符个数小于20,因此本行等价于第7行。笫5行输出 str 的内容。第6行,将 str 字符串的前10 个字符复制到 str2 中。由于此时 str 字符串的内容是"hell0 world"字符数大于10,因此只把前10 个字符复制到 str2 中。此时 str2 中缺少字符串结束符,str2 是-个字符序列,并不是字符串。第7行 如果不注释本行会引发运行时错误因为 str2 不是一个合法字符串。函数会从 str2 的第个字符开始直输出, 直到遇到结束符,但 str2 所有的 10 个元素都不含结束符。从而导致数组越界访问,结果不可预料。Prinff