7.4.2指针与下标数组名作为表达式'可以看作指向数组第一个元素的指针但数组名是常量,不能被赋值或修改其值a6000P*p 三 a[0]6001a [0]因此指针可赋值为数组名60026003int *p, 3[10];a+16004P++*p = 3[1]6005p = a;//p指向数组a的第一个元素6006[1]6007数组元素的几种等价引用形式a+2Pt+*三a[2]60086009[2]a[i]数组名+下标6010*(a+i)数组名+偏移量,最后解引用6011p[]指针+下标p =a;a =p ;*(pti)指针+偏移量最后解引用a++;关于数组和指针关系的进步阐述。详见下一章节