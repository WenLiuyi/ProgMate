8.7.2函数指针用祛与惠义例 8-13通用冒泡排序函数 写一个通用的冒泡排序函数,可以对任何数据类型的数组进行任何形式 (升序或降序)  的稳定排序,具体的数组元素比较行为通过函数指针实现。比较函数需要满足严格有序条件。即,设 comp(el, e2)是一个比较函数,则它必须满足:若 e1先于 e2则 comp(el, e2)的值为真;若 e1 后于 e2 则 comp(ez, e1)的值为真;若e1和 e2 顺序相当,则 comp(el, e2)和 comp(ez, e1)的值同时为假。可以验证,less 和 greater 函数均满足严格有序条件。否则,可能造成不稳定排序。如果将|ess函数中的小于号写成小于等于则不满足严格有序条件中的第@条。在进行冒泡排序时。如果 j元素和 j+1元素相等sort 函数在调用less 函数会返回真   从而进行元素值的交换。这显然是一种不稳定排序。intlessconstint *e1,Constint*e2)return*e1<二*e2;不满足严格有序条件g_bub