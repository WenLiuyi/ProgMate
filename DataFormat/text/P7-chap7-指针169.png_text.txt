7.4.4其他指针运算除了上述合法的指针运算外;其他关于指针的数学运算均为非法。包括指针乖法指针与浮点数的加减指针之间的相加:。移位运算等。编译器均会报错,因为这些运算没有实际的物理意义。指针可以参与逻辑运算当指针为 NULL 指针时表示逻辑假;否则表示逻辑真。char5[180],t[10];char*pstrstr(s,t);if (p) 等价于 if (p != NULL)。if(p)11如果在 s中找到七else