83.1二维数组的深入理解例 &-5 按字符串长度排席(二维数组版)  在标准输入上读入多行字符串 (不超过 100行每行不超过1000 个字符每行的字符中可以包含空格)按照从短到长的顺序在标准输出上打印每行内容如果长度样则按字典序从小到大排列。for(1=1; 1 < k;i++)11bubblesortingfor(j= 0; i<k -1;j++)接上页int dicflagstrlen(a[j])三三strlen(alj十1] )&& strcmp(a[j],a[j十1]) >0;讦(stnlen(alj])〉strlen(alj十1])|dicflag)strcpy(tmp,a[j]);strcpy(a[j],a[i十1] );strcpy(a[j十1],tmp) ;本例中,冒泡排序的元素交换环节需要进for(1二0;1 < k;++i)行三次字符串的整体数据复制效率较低。fputs(a[1],stdout);第 8.5节给出了一个更高效的算法。return0;