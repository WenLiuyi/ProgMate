6.1.5 sizeof 应用于数组sizeof 应用于数组数组在内存占用一片连续的存储区。sizeof 运算符可以用于数组变量(数组名是数组类型的变量)获取数组所占的内存空间大小 (以字节为单位)1数组所占字节数等于数组长度(数组元素个数)乖以单个元素所占字节数。1 int a[6];编程技巧2 double b[6];定义一个通用的宏函数来方便获取数组长度3 char c[6];4Printf("Ood %d %odln'Sizeof(a), sizeof(b), sizeof(c))#define ARRAY_LEN(X ) (sizeof(x)Isizeof(x[0]))输出结果二int a[10],1;24486double b[25];char c[120];for(i=0;1ARRAY_LEN(a); i++ ) // 处理数组a的相关代码a占24个字节6占48个字节c占6个字节for(i=0;1ARRAY_LEN(b); i++ ) /1 处理数组6的相关代码for(i=0;1< ARRAYLEN(c); i++ ) //处理数组c的相关代码