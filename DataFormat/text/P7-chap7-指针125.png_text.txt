讥 Iine边line读入新行#include<stdio.h>longest当前最长行longest当前最长行#includecstring.h>#defineMAX100边 lie读入新行边当前最长新行intmain()longest当前最长行longestchararr1 [MAX],arr_2 [MAX]char*inIineCarr_I,米longestCarr_2, *tmp;初始化in_line和longestintmaxlen0,len;While(gets(in_line)! =NULL )初始化指针使其分读入新行别指向一块空间lenstrlen(in_line);是if(lenmax_len)判断是否到读完保存新行长度maXlenClen;否tmp三line;否判断新行是否inlineIongest;交换指向当前行和比当前最长行长longestCtmp;所保存行的指针是交换in_line和longestprintf("%d: %s In"max_len,longest);输出10n8est指向的行linein_-