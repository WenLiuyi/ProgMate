switch语句实例:成绩分段输出例4-81输入一个成绩 (百分制整数) ,转换为等级制(A~卫)#include <stdio.h>switchscorell0 )int main0case 10:I1e.8. 100/10is 10Case 9:96/10is 9it score;Printf("Input score(0~100): ");Printf("AIn");break;scanf("Od" , &score);Case 8:Printf("Bln");i(!(score >= 0 && score <= 100))break;Case 7:Printf("invalid input! quit!");Printf("CI");retun 1;break;Case 6:1 <未完。转右边〉Printf("DWn");break;若连续的case之间没有语句则它们执行同样的语句。default:Printf("Fln");与所有case都不匹配时执行default对应的语句。break;break语句使程序跳出switch/Lend switchcase中有多个语句时不必放在{}中。return 0;switch只用于测试整型值。11e.8.